# macOS
.DS_Store
*.swp

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.coverage
htmlcov/
.venv/
venv/

# Node
node_modules/

# Frontend build artifacts (never commit; produced by `npm run build`)
frontend/dist/
frontend/.vite/
playwright-report/
test-results/

# SME-authoring runtime state (lock files + reservations are session-local)
tests/clinical/.id_allocator.lock
tests/clinical/.id_allocator.reservations

# IDE
.vscode/
.idea/
*.iml

# Local working files (not for the public repo)
_local_staging/
_planning/
todo/
Publications/

# Working artifacts (kept locally, not in repo)
PACCA_PRD_Consolidated.docx
PACCA_PRD_Consolidated.pdf
PACCA_SDD_v2.2.pdf
*.jpg
*.jpeg
*.png
!docs/**/*.png
!docs/**/*.jpg

# The commit script itself (it's a one-time tool, not part of the repo)
phase_h0_commit_sequence.sh

# Logs
*.log

# Environment
.env
.env.local

# Local databases (never commit; may contain test PHI)
*.db
*.sqlite
*.sqlite3
pacca.db
pacca_db/
src/pacca/api/pacca_db/
.DS_Store
